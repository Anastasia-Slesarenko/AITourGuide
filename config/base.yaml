# Базовая конфигурация приложения.
# Все параметры управляются через переменные окружения (.env).
# Этот файл зарезервирован для будущего использования с YAML-конфигом.
# Текущая конфигурация: src/core/config.py (pydantic-settings).
