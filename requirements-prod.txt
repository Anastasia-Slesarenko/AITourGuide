# ============================================================
# Production dependencies for the FastAPI api container.
# Does NOT include training, evaluation, or data-preparation
# packages (bert-score, peft, mlflow, datasets, boto3, etc.)
#
# Install: pip install -r requirements-prod.txt
# ============================================================

# ── Deep Learning (inference only) ───────────────────────────
torch==2.2.2
torchvision==0.17.2   # Зафиксировано: >=0.17.2 могло подтянуть несовместимую версию
transformers==4.40.0
numpy<2.0.0          # NumPy 2.x incompatible with PyTorch 2.2.2
sentencepiece>=0.1.99 # Required by SiglipTokenizer
protobuf>=3.20.0      # Required by SiglipTokenizer (alongside sentencepiece)

# ── Vision ───────────────────────────────────────────────────
Pillow==11.3.0

# ── RAG / Vector search ──────────────────────────────────────
faiss-cpu==1.8.0
pandas>=2.0.0        # Required by indexing_v2.py (build_from_dataframe)

# ── HTTP clients ─────────────────────────────────────────────
httpx>=0.27.0        # VLLMClient in ai_tour_guide.py
aiohttp>=3.9.0       # WikipediaService in yandex_search.py
requests>=2.31.0     # YandexSearchService (sync fallback)

# ── Async / utils ────────────────────────────────────────────
aiofiles>=23.2.1
cachetools>=5.3.0
tqdm==4.67.1

# ── FastAPI & web server ──────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
jinja2>=3.1.0

# ── Monitoring ────────────────────────────────────────────────
prometheus-client>=0.20.0        # Prometheus metrics exposition (/metrics)

# ── Configuration ─────────────────────────────────────────────
python-dotenv>=1.0.0
PyYAML>=6.0.1
