# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
pytest-timeout>=2.1.0
httpx>=0.27.0              # For FastAPI testing (aligned with prod >=0.27.0)
faker>=19.0.0              # For generating test data

# ── Code Quality ──────────────────────────────────────────────────────────────
black>=23.7.0              # Code formatter
mypy>=1.5.0                # Type checker
ruff>=0.4.0                # Linter + import sorter (replaces flake8 + isort)

# ── Type Stubs ────────────────────────────────────────────────────────────────
types-requests>=2.31.0
types-PyYAML>=6.0.12
# types-Pillow is no longer maintained (Pillow 10+ ships inline stubs)
types-cachetools>=5.3.0

# ── Development Tools ─────────────────────────────────────────────────────────
ipython>=8.14.0            # Enhanced Python shell
jupyter>=1.0.0             # Jupyter notebooks
notebook>=7.0.0            # Jupyter notebook server
ipykernel>=6.25.0          # Jupyter kernel
ipywidgets>=8.1.0          # Interactive widgets

# ── Debugging & Profiling ─────────────────────────────────────────────────────
pdbpp>=0.10.3              # Enhanced debugger
line-profiler>=4.0.0       # Line-by-line profiling
memory-profiler>=0.61.0    # Memory profiling
py-spy>=0.3.14             # Sampling profiler

# ── Documentation ─────────────────────────────────────────────────────────────
mkdocs>=1.5.0              # Documentation generator
mkdocs-material>=9.2.0     # Material theme for MkDocs
mkdocstrings>=0.22.0       # Auto-generate docs from docstrings
mkdocstrings-python>=1.5.0 # Python handler for mkdocstrings

# ── Monitoring & Observability ────────────────────────────────────────────────
prometheus-client>=0.20.0  # Aligned with prod requirement

# ── Pre-commit Hooks ──────────────────────────────────────────────────────────
pre-commit>=3.3.0          # Git pre-commit hooks

# ── Security ──────────────────────────────────────────────────────────────────
bandit>=1.7.5              # Security linter
safety>=3.0.0              # Dependency vulnerability scanner (3.x: `safety scan`)

# ── Performance Testing ───────────────────────────────────────────────────────
locust>=2.15.0             # Load testing framework

# ── Training & Experiments ────────────────────────────────────────────────────
# Install torch separately before this section:
#   CPU:  pip install torch==2.2.2 torchvision==0.17.2
#   GPU:  pip install --index-url https://download.pytorch.org/whl/cu121 \
#             torch==2.2.2 torchvision==0.17.2
peft>=0.18.0               # LoRA fine-tuning (experiments/train.py, e2e_eval*.py)
datasets>=2.19.0           # HuggingFace datasets
mlflow>=2.11.0             # Experiment tracking (experiments/train.py, eval.py)

# ── Experiment Analysis & Metrics ─────────────────────────────────────────────
scikit-learn>=1.3.0        # f1_score, roc_auc, calibration (experiments/eval.py etc.)
scipy>=1.11.0              # optimize, special (experiments/calibrate_and_select.py)
matplotlib>=3.7.0          # Plotting (experiments/plotting.py, eval.py)
rouge-score>=0.1.2         # ROUGE metrics (experiments/eval.py)

# ── Data Preparation ──────────────────────────────────────────────────────────
qwen-vl-utils>=0.0.8       # Qwen VL utilities (step4a, step4b)
boto3>=1.34.0              # AWS S3 client (upload_dataset_s3.py, download_dataset_s3.py)

# ── VLM Inference (llama.cpp) ─────────────────────────────────────────────────
llama-cpp-python>=0.2.0    # Qwen2-VL inference via GGUF (experiments/test_gguf_model.py)
